1. Пагинация

2. При переходе на конкретный товар дропается ошибка, думаю нужно исправлять через редакс, обрабатывать ошибки внутри. Проблема в том, что при рендере страницы еще не успевает за сетстейтиться данные 

    \*\* Исправил, возможно, временно добавлением reloadDocument в Link при нажатии на товар на главной странице

3. Корзина

    \*\* Сделал запрос на получение товаров из БД. Связал нужные таблицы. На фронте доделать суммирование и добавление в редакс, для последующего удаления
    \*\* Проблемы - 1. Нужно сделать удаление товаров из корзины
    \*\* 2. Нужно сделать добавление товаров в корзину
    \*\* 3. Нужно сделать, чтобы можно было менять количество товара в корзине

4. Админ панель:
    - Получения списка пользователей с ролями (добавление/изменение ролей) + Удаление пользователей

    \*\* Добавил получение списка пользователей по id/role
    \*\* Проблемы - 1. Нужно дописать логику по удалению пользователей

    - Изменение товара (имя, бренд, категорию, цену, картинку и тд)
    
    \*\* Добавил логику на изменение товара. Сейчас можно выбрать имя товара, которое хотим изменить и в форме появятся данные, которые внесены в БД у этого товара.
    \*\* Проблемы - 1. Нужно в контроллере сджойнить к запросу таблицы с типами и брендами, чтобы на клиенте можно было вытащить названия (сейчас только айди)
    \*\* 2. Не приходит информация и хайлайты товара. Проблему нужно искать в запросе в контроллере, возможно надо добавить дополнительное where
    \*\* 3. Не понятно как вытаскивать картинку из бд и пихать ее в инпут, чтобы была возможность оставить как есть или заменить. Возможно нужно никак не отрисовывать, а просто сделать
    \*\* дополнительную проверку на сервере, что если от клиента приходит null у img, то оставлять данные, которые были в бд, иначе - обновлять на новую картинку.

    - Удаление товара

    UPD2:
    \*\* Доделал весь клиент на изменение товара, кроме картинки (нужно подумать как обрабатывать). 
    \*\* Проблемы - 1. Не подготовил запрос и форму аппенд, для отправки на сервер (не забывать передавать deviceId)
    \*\* 2. На сервере сделал правильный запрос (БД обновляется), возвращается ответ, но с NULL в начале (добавил returning: true, plain: true - нужно подробнее почитать про отправку ответа)
    \*\* 3. На сервере нужно дописать логику по картинке и проверить (не тестил отправку даже) обновление информации.

5. Переходы по категориям и брендам (на главной и в device info)

    \*\* Фильтрация отправляется на сервер, там все работает и сортирует. 
    \*\* Проблемы - 1. На фронте возникли проблемы с навигейтом при выборе чекбокса. 
    \*\* Возможные решения - прогнать через редакс (проблема в том, что сетстейт асинхронный и приходит запрос после чекбокса). 
    \*\* Функция для получения url отрабатывает правильно, в useeffecte - опаздывает. // приходит правильная ссылка, нужно ее как-то впихнуть в navigate или во что-то другое. 
    \*\* Посмотреть на проект Cocktail Party, там попробовать разобраться с применением бибилотеки qs и ссылкой на Ref

    UPD2:
    \*\* Сделал отправку запросов по категориям товаров, с брендами так и не нашел решения. Самое близкое было со сетстейтом, но из-за ассинхронности запаздывалось отображение линка на 1.
    \*\* Проблемы - 1. Нужно подправить мобильную версию для категорий, не переходит по категориям и не скрывается попап боковой.
    \*\* 2. Когда перехожу по категориям и нажимаю на товар, чтобы перейти на подробную информацию - перекидывает на главную. Поправить линк.

6. Сортировка

8. Оптимизация ререндеров страницы (чтобы кешировались данные)

9. Скелетон для товаров и нормальный спиннер для загрузки (вместо Загрузка...)

\*\* 7. Попробовать реализовать оплату или хотя бы посмотреть как это делается


Рабочий запрос на JOIN по fk:

SELECT d.id, d.name, d.price, d.img, t.name, b.name  FROM devices d
JOIN types t ON t.id = "typeId"
JOIN brands b ON b.id = "brandId"
ORDER BY d.id

-- GET запрос на корзину

select b.id as "basketId", d.id as "deviceId", d.name as "deviceName", d.price as "devicePrice", d.img as "deviceImg" from basket_devices bd
JOIN baskets b ON b.id = "basketId"
JOIN devices d ON d.id = "deviceId"
ORDER BY b.id DESC

-- 
